# NOTE: opencv-python-headless 5.0.0 removed/broke the HOGDescriptor and
# CascadeClassifier Python bindings this project depends on -- pinned
# below 5.0 deliberately. Tested against 4.13.0.92.
opencv-python-headless>=4.8,<5.0
numpy>=1.24
scikit-learn>=1.3
joblib>=1.3
pytest>=7.4
